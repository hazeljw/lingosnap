{
  "name": "@lingosnap/shared",
  "version": "1.0.0",
  "description": "Shared types and data for LingoSnap client and server",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "prepare": "npm run build",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  },
  "files": [
    "dist/**/*",
    "data/**/*"
  ]
} 